<div class="container">
  <h1>New task</h1>

  <%= form_for(@task, html: { id: 'new_form'}) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_field :description %>

    <%= f.submit class: 'blue-btn' %>
  <% end %>
</div>
